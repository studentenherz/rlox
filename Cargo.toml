[package]
name = "rlox"
version = "0.9.0"
edition = "2024"
authors = ["Michel Romero"]

[dependencies]
clap = { version = "4.5.53", features = ["derive"] }
rustyline = "17.0.2"

[profile.release]
codegen-units = 1
debug = false
lto = true
panic = "abort"
strip = true
